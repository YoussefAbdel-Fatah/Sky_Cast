package com.example.skycast.presentation.theme

import androidx.compose.ui.graphics.Color

// Primary Accents
val SkyBlue = Color(0xFF62B7F6) // The blue from the active card and icons
val SkyBlueLight = Color(0xFFE1F2FF) // Optional: for lighter blue backgrounds

// Backgrounds & Surfaces
val BackgroundLight = Color(0xFFF8F9FA) // Slight off-white for the main background
val SurfaceWhite = Color(0xFFFFFFFF) // Pure white for the cards

// Typography Colors
val TextPrimary = Color(0xFF1E232C) // Dark slate for temperatures and titles
val TextSecondary = Color(0xFF838BA1) // Gray for labels like "HUMIDITY" and "Partly Cloudy"